- name: Ensure nginx is installed
  ansible.builtin.package:
    name: nginx
    state: present
    update_cache: yes
  become: yes

- name: Run nginx
  ansible.builtin.shell: nginx -g "daemon on;"
  become: yes
